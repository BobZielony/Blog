import { SideBar } from "./SideBar"
export const Header = () => {
    return (
        <div className="header">
            <SideBar/>
            <p>Blog o tematyce różnej</p>
        </div>
    )
}